services:
  client:
    image: ${DOCKERHUB_USERNAME}/louisplace-client:latest
    container_name: next_app
    ports:
      - "80:3000"
    environment:
      NODE_ENV: production
    restart: always
